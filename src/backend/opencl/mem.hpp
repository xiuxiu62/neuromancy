#pragma once

// #include "backend/opencl.hpp"
// #include "core/types.h"

// #include <CL/cl.h>

// struct MemoryInfo {
//     usize total_memory;     // Total VRAM on device
//     usize available_memory; // Currently available VRAM
//     usize max_allocation;   // Maximum single allocation size
// };

// bool init_safe(OpenCLContext &ctx, OpenCLNetwork &network, const Network &host_network);
// MemoryInfo query_gpu_memory(OpenCLContext &ctx);
// bool check_memory_requirements(OpenCLContext &ctx, usize required_memory);
